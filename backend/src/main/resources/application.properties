# backend/src/main/resources/application.properties

# --- Active Profile ---
spring.profiles.active=dev

# --- Server Port ---
server.port=8080

# --- App Info ---
spring.application.name=sportfacilitybooking

# --- Local SQL Server Connection ---
spring.datasource.url=jdbc:sqlserver://huyht.database.windows.net:1433;database=sport_facility_booking_sys;\
                      user=huyht149@huyht;password=Hoangthaihuy12!;\
                      encrypt=true;\
                      trustServerCertificate=false;\
                      hostNameInCertificate=*.database.windows.net;\
                      loginTimeout=30;

spring.datasource.username=huyht149
spring.datasource.password=Hoangthaihuy12!
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- Local SQL Server Connection ---
# spring.datasource.url=jdbc:sqlserver://DUCK:1433;\
#   databaseName=sportfacilitybooking_db;\
#   encrypt=true;\
#   trustServerCertificate=true;\
#   loginTimeout=30

# spring.datasource.username=sa
# spring.datasource.password=123
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.validator.apply_to_ddl=false

# --- Disable automatic naming strategy to match existing DB schema ---
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# --- Flyway Migration ---
spring.flyway.locations=classpath:db/migration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# --- Logging Configuration ---
logging.level.root=INFO
logging.level.group6.it.ou.sportfacilitybooking=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n
logging.file.name=logs/sportfacilitybooking.log

# --- VNPay Sandbox Configuration ---
# Replace with your sandbox merchant values from VNPay.
vnpay.tmn-code=4WWSAFRB
vnpay.hash-secret=4V45G7KLG5QIZRNA49T0SPDMCGIXOLPK
vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.return-url=http://localhost:3000/payment/vnpay-return
vnpay.version=2.1.0
vnpay.command=pay
vnpay.curr-code=VND
vnpay.locale=vn
vnpay.order-type=other

# --- Cloudinary Configuration ---
# Get your credentials from https://cloudinary.com/console
cloudinary.cloud-name=dd1frsvzk
cloudinary.api-key=172266497937733
cloudinary.api-secret=W_FvA2NSah3Jlv8cxhubvnw2mVM

# --- File Upload Configuration ---
file.upload.max-size=10485760
file.upload.allowed-types=image/jpeg,image/png,image/gif,image/webp